#include <stdio.h>
#include <math.h>

using namespace std;

int main(){
	int n;
	float f;
	while(scanf("%d", &n),n){
		f=(-1+sqrt(1-4*n))/2;
		
		if(ceil(f)==(int)f){
			printf("YES\n");
		}else{
			printf("NO\n");
		}
	
	}
}